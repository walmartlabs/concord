flows:
  default:
    - log: "default: ${currentFlowName()}"
    - call: myFlow

    - checkpoint: "first"
    - log: "after checkpoint: ${currentFlowName()}"

  myFlow:
    - log: "myFlow: ${currentFlowName()}"
