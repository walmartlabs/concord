flows:
  default:
    - set:
        items: [1,2,3]

    - call: test
      in:
        item: ${item}
      withItems: ${items}

  test:
    - set:
        arr: []
    - log: "empty: ${arr}"
    - ${arr.add(item)}
    - log: "after add: ${arr}"