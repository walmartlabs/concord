configuration:
  runtime: concord-v2.5
forms:
  approval:
    - choice:
        type: string
flows:
  default:
    - parallel:
        - block:
            - form: approval
        - block:
            - form: approval
