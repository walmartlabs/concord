configuration:
  runtime: concord-v2.5
flows:
  default:
    - expr: ${missingValue}
  guarded:
    - out: present
      expr: ${hasVariable('missingValue')}
    - log: "v25-strict-expression guarded=${present}"
