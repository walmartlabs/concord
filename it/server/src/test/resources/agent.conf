concord-agent {

    dependencyResolveTimeout = "30 seconds"
    logMaxDelay = "250 milliseconds"
    pollInterval = "250 milliseconds"

    prefork {
        enabled = true
    }

    git {
        allowedSchemes = ["file","ssh","classpath"]
    }

    capabilities = {
        type = "test"
    }

    server {
        apiKey = "cTJxMnEycTI="
        processRequestDelay = "250 milliseconds"
    }
}
