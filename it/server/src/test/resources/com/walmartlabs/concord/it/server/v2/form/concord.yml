runtime: concord-v2

flows:
  default:
    - form: myForm
      fields:
        - firstName: { type: "string" }
        - lastName: { type: "string" }
        - age: { type: "int" }

    - log: "myForm: ${myForm}"