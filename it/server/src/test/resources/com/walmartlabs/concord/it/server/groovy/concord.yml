configuration:
  dependencies:
    - "mvn://org.codehaus.groovy:groovy-all:pom:3.0.11"

flows:
  default:
    - script: groovy
      body: |
        def username = initiator.username;
        println "Hello, ${username}";
