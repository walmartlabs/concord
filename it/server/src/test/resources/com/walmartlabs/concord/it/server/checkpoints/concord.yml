configuration:
  runner:
    events:
      recordTaskOutVars: true # test uses out vars to verify checkpoints

flows:
  default:
    - log: "==Start"
    - checkpoint: "ONE"
    - log: "==Middle"
    - checkpoint: "TWO"
    - log: "==End"