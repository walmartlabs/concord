configuration:
  dependencies:
    - "mvn://org.codehaus.groovy:groovy-all:pom:3.0.11"

flows:
  default:
    - script: groovy
      body: |
        import com.walmartlabs.concord.plugins.example.ExampleBean
        execution.setVariable("x", new ExampleBean("hello"))

    - form: myForm

    - log: "${myForm.y}"

forms:
  myForm:
    - y: { type: "string" }
