configuration:
  arguments:
    useForm: false

forms:
  releaseForm:
  - x: { type: "string" }

flows:
  default:
  - ${lock.lock("my-lock", "ORG")}
  - log: "locked!"
  - if: ${useForm == true}
    then:
    - form: releaseForm
  - ${lock.unlock("my-lock", "ORG")}
