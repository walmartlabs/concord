---
- hosts: aaa
  tasks:
    - debug:
        msg: "{{ msg }}"
        verbosity: 0

- hosts: bbb
  tasks:
    - debug:
        msg: "{{ msg }}"
        verbosity: 0

- hosts: ccc
  tasks:
    - debug:
        msg: "{{ msg }}"
        verbosity: 0
