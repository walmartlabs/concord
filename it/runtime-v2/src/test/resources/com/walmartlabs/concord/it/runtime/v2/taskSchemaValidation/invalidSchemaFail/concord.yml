configuration:
  runtime: "concord-v2"
  dependencies:
    - "mvn://com.walmartlabs.concord.it.tasks:schema-test:PROJECT_VERSION"
  validation:
    taskCalls:
      in: fail

flows:
  default:
    - task: invalidSchema
      in:
        message: "hello"
    - log: "Done"
