configuration:
  runtime: "concord-v2"
  arguments:
    aVar:
      x: 123

flows:
  default:
    - log: "#1 ${aVar}"
    - checkpoint: "first"
    - set:
        aVar.y: 234
    - log: "#2 ${aVar}"
    - checkpoint: "second"
    - set:
        aVar.y: 345
    - log: "#3 ${aVar}"
