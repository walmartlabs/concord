configuration:
  dependencies:
  - "mvn://com.walmartlabs.concord.plugins.basic:ansible-tasks:0.51.0"

flows:
  default:
  - task: ansible
    in:
      inventory:
        local:
          hosts:
            - "127.0.0.1"
          vars:
            ansible_connection: "local"
      playbook: playbook/hello.yml
      extraVars:
        greetings: "Hi there!"
