configuration:
  dependencies:
  - "mvn://com.walmartlabs.concord.plugins:looper-task:0.53.1"

flows:
  default:
    - task: looper
      in:
        username: "${initiator.username}"
        apiToken: "LOOPER_API_TOKEN"
        jobName: "my-job"
