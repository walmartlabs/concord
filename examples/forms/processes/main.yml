main:
- form: myForm
- ${log.info('test', 'Hello, '.concat(myForm.firstName).concat(' ').concat(myForm.lastName).concat(' ' ).concat(myForm.password))}

form (myForm):
- firstName: { label: "First name", type: "string" }
- lastName: { label: "Last name", type: "string" }
- password:  { label: "Password", type: "string", inputType: 'password' }