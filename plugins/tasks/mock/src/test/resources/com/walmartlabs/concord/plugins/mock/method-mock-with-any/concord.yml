flows:
  default:
    - expr: "${undefinedTask.myMethod(1, 'something')}"
      out: result

    - log: "result.ok: ${result}"

configuration:
  arguments:
    mocks:
      - task: "undefinedTask"
        method: "myMethod"
        args: [1, "${mock.any()}"]
        result: "BOO"
