flows:
  default:
    - set:
        mocks:
          - task: git
            in:
              action: "clone"

    - task: git
      in:
        action: "clone"

    - expr: "${verify.task('git', 1).execute({'action': 'clone'})}"
