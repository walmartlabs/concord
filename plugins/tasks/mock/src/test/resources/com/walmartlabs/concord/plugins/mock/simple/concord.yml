flows:
  default:
    - task: undefinedTask
      out: result

    - log: "result.ok: ${result.ok}"

configuration:
  arguments:
    mocks:
      - task: "undefinedTask"
        out:
          ok: true
